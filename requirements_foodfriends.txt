# Python dependencies for foodfriends project
# Frozen from the working server venv 2026-09-03 (CPython 3.11). Full transitive closure.
#
# Reinstall with:
#   uv venv --python 3.11
#   uv pip install -r requirements_foodfriends.txt --no-deps
#
# --no-deps is required and safe here: netin 1.0.7 declares obsolete bounds
# (numpy<1.25, matplotlib<3.8) that strict resolvers reject, and a full freeze
# already pins every real dependency, so nothing is missed.
# pip inside the conda env tolerates the netin bounds with a warning; uv does not.

bounded-pool-executor==0.0.3
contourpy==1.3.3
cycler==0.12.1
fonttools==4.64.0
kiwisolver==1.5.1
matplotlib==3.11.1
mpmath==1.3.0
netin==1.0.7
networkx==3.1
numpy==1.26.4
packaging==26.3
pandas==3.0.5
pillow==12.3.0
powerlaw==2.0.0
pqdm==0.2.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
scipy==1.12.0
seaborn==0.12.2
six==1.17.0
sympy==1.14.0
tqdm==4.70.0
typing-extensions==4.16.0
